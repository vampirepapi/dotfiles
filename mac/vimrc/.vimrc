" Enable syntax highlighting
syntax on

" Show line numbers
set number

" Set UTF-8 encoding
set encoding=utf-8

" Tabs and indentation for Java
set tabstop=4
set shiftwidth=4
set expandtab
set autoindent
set smartindent

" Enable incremental search and highlight matches
set incsearch
set hlsearch

" Enable mouse support
set mouse=a

" Show ruler (line and column number)
set ruler

" Set filetype detection, plugins, and indentation
filetype plugin indent on

" Set clipboard to use system clipboard (optional, works on macOS)
set clipboard=unnamed,unnamedplus

